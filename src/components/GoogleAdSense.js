/**
 * GoogleAdSense Component
 *
 * Placeholder component for pages that should display Google AdSense ads.
 *
 * The AdSense script is now loaded globally in index.html for site verification.
 * This component serves as a marker to identify which pages should display ads.
 *
 * Usage: Add <GoogleAdSense /> to content pages where ads should appear:
 * - Blog.js
 * - BlogPost.js
 * - ForTheRecord.js
 * - Community.js
 */
export default function GoogleAdSense() {
  // The script is already loaded in index.html
  // This component is just a placeholder/marker for ad-enabled pages
  // Google AdSense will automatically detect and serve ads on these pages

  // This component doesn't render anything visible
  return null;
}

import React from "react";
/**
 * PlayerProgress - Progress bar for player.
 */
export default function PlayerProgress({ progress, duration, onSeek }) {
  return <div>{/* TODO: Implement PlayerProgress */}</div>;
}

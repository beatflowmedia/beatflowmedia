import React from 'react';
import PlayerProgress from './PlayerProgress';
export default {
  title: 'Molecules/PlayerProgress',
  component: PlayerProgress
};
export const Default = () => <PlayerProgress progress={} duration={} onSeek={} />;

import React from 'react';
import TrackRow from './TrackRow';
export default {
  title: 'Molecules/TrackRow',
  component: TrackRow
};
export const Default = () => <TrackRow track={} onPlay={} onAddToPlaylist={} />;
